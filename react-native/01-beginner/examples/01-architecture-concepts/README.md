# 01 — Architecture Concepts

ตัวอย่างประกอบทฤษฎี Bridge / JSI / New Architecture
ไฟล์นี้รันเป็นหน้าจออธิบายใน Expo ได้ หรืออ่านอย่างเดียวก็ได้

## วิธีทดลอง

```bash
npx create-expo-app@latest arch-demo --template blank-typescript
# คัดลอกไฟล์ใน folder นี้เข้าไปแล้วตั้งเป็น entry
npx expo start
```
