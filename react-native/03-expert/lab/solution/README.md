# เฉลย Lab Expert — StockPulse

ติดตั้งใน project Expo:

```bash
npx expo install @shopify/flash-list expo-sqlite expo-local-authentication expo-secure-store expo-updates
```

โครงสร้างเฉลยรวมแกนสำคัญไว้ใน folder นี้ — คัดลอกเข้าแอปแล้วผูกกับ Expo Router ตามที่ต้องการ
