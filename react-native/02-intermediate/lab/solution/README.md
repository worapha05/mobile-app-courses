# เฉลย Lab Intermediate — FieldShelf

คัดลอกโครง `app/`, `lib/`, `hooks/`, `components/` เข้า project Expo Router (template `tabs`)
ติดตั้ง dependencies ที่เกี่ยวข้อง เช่น:

```bash
npx expo install expo-secure-store expo-camera expo-location @tanstack/react-query
```
